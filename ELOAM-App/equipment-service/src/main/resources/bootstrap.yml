spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  application:
    name: equipment-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    dbname: ELOAM-equipment
    url: jdbc:postgresql://$localhost:5432/ELOAM-equipment?autorReconnect=true&allowPublicKeyRetrieval=true&useSSL=false
    username: postgres
    password: 1502