spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  application:
    name: teacher-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ELOAM-teacher?autorReconnect=true&allowPublicKeyRetrieval=true&useSSL=false
    username: postgres
    password: 1502